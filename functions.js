let name = 'Ain'
let number = square(3,3);

//performing a task
function greet(name) {
    console.log('Hello ' + name);
    console.log('This is the number ' + number)
}
greet (name);

//calculating a value
function square(number, number) {
    return number*number;
}

square(number);